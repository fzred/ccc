{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ccc",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/a.out",
            "args": ["test/test.ccc"],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "make",
            "MIMode": "lldb"
        }
    ]
}